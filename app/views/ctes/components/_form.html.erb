<div class="col-lg-12">
  <%= form.label('Número do CT-e') %>
  <%= form.number_field(:cte_number, class: 'form-control form-control-user', required: true) %>
</div>
<div class="col-lg-12">
  <%= form.label('Valor do CT-e') %>
  <%= form.text_field(:value, id: 'money', class: 'form-control form-control-user input--money', required: true) %>
</div>
<div class="col-lg-12 d-flex">
  <div class="col-lg-10">
    <%= form.label('Cargas') %></br>
    <%= form.collection_select(:truckload_id, @truckloads, :id, :formatted_truckload, {}, { class: 'select2_single form-control', required: true }) %>
  </div>
  <div class="col-lg-2 m-t-5">
    <% if can?(:create, Truckload) %>
      <%= link_to new_truckload_path(@truckload), class: 'btn btn-sm btn-success btn-circle' do %>
        <i class="fas fa-plus"></i>
      <% end %>
    <% end %>
  </div>
</div>
